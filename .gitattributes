# Normalise line endings so Windows checkouts do not produce CRLF diffs.
* text=auto eol=lf

*.png binary
*.ico binary
*.icns binary

# Keep the lockfile out of diffs and language statistics.
package-lock.json -diff linguist-generated
